This project is a simple interactive To-Do List built with JavaScript.

Users can add tasks, click on them to mark as completed, and the list is saved using localStorage. This means the tasks remain even after refreshing the page.

The page uses event listeners to respond to user actions and dynamically updates the DOM.

I chose Option A (Local Storage) to store and retrieve user data.
